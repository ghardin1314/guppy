{
  "name": "@guppy/cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "guppy": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "echo no tests"
  },
  "dependencies": {
    "@guppy/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
