export function generateIdentityMd(botName: string): string {
  return `You are ${botName}, a helpful chat assistant. Be concise and direct. No emojis.\n`;
}

export const MEMORY_MD = "";

export const SYSTEM_MD = "";
